# 04-RAG · 面试题

## 题目一（高频）

> "请讲讲 RAG 的完整流程，从用户提问到最终回答经历了哪些步骤？"

### 考点（面试官会层层追问）

1. RAG 的五大步骤：加载 → 分块 → 嵌入 → 检索 → 生成
2. 为什么需要分块？分块策略有哪些？（固定长度、语义分块、递归分块）
3. 嵌入模型和生成模型的区别？
4. 向量检索的原理？用什么数据库？（Chroma / Milvus / FAISS）
5. 检索到的文档怎么拼进 Prompt？上下文窗口不够怎么办？

### 我的回答（学完本项目后，用自己的话写在这里）

> （做完 `mini_rag/` 项目后，在这里用大白话复述一遍）

---

## 题目二（高频）

> "RAG 系统效果不好，你会怎么排查和优化？"

### 考点

1. 检索质量差的原因？（分块太大/太小、嵌入模型不匹配、query 改写不足）
2. 生成质量差的原因？（上下文噪声、Prompt 模板问题、模型能力不足）
3. 有哪些优化手段？（Rerank 重排序、HyDE、混合检索、多粒度分块）
4. 怎么评估 RAG 效果？（忠实度、相关性、RAGAS 框架）

### 我的回答

>

---

## 题目三（中频）

> "什么是向量嵌入（Embedding）？为什么能用向量搜索来做语义检索？"

### 考点

1. 嵌入的本质是什么？（把文本映射到高维向量空间）
2. 语义相似的文本，向量有什么特点？（余弦相似度高）
3. 嵌入模型和 LLM 的区别？
4. 常用的嵌入模型有哪些？怎么选？

### 我的回答

>

---

## 题目四（中频）

> "Rerank 重排序是什么？为什么检索之后还要再排一次？"

### 考点

1. 向量检索的局限性？（语义匹配 ≠ 相关性排序）
2. Rerank 模型和嵌入模型的区别？（交叉编码器 vs 双塔编码器）
3. Rerank 放在 RAG 流程的哪个位置？
4. 混合检索（BM25 + 向量）和 Rerank 的关系？

### 我的回答

>

---

## 题目五（加分）

> "什么是 HyDE（假设性文档嵌入）？它解决什么问题？"

### 考点

1. 用户 query 和文档之间的语义鸿沟是什么？
2. HyDE 的核心思路：先让 LLM 生成假设性答案，再用答案去检索
3. HyDE 的优缺点？什么场景适用？
4. HyDE 和 Query Rewriting 的区别？

### 我的回答

>

---

## 出处
- 代码随想录《大模型面经汇总》RAG 专题
- 严灿平《RAG 实战》全书
- RAGAS 评估框架文档
